{
  "name": "designos",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend/shell run start",
    "build": "npm --prefix frontend/shell run build",
    "preview": "npm --prefix frontend/shell run build",
    "legacy:dev": "vite",
    "legacy:build": "vite build",
    "legacy:preview": "vite preview",
    "backend:dev": "npm --prefix backend run start:dev"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "typescript": "^5.5.0"
  }
}
